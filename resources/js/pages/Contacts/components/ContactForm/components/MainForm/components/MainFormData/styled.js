import styled from 'styled-components';

const MainFormData = styled.div`
  margin-left: 1.5rem;
  width: 100%;
`;

const FieldContainer = styled.div`
  display: flex;
  margin: 0.25rem 0;
`;

export default { MainFormData, FieldContainer };
